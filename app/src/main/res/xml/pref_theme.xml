<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <ListPreference
        android:defaultValue="100"
        android:entries="@array/pref_theme_color_name"
        android:entryValues="@array/pref_theme_color_values"
        android:key="theme_color_pref"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_theme_color"
        android:summary="@string/pref_summary_set_color"
        />

</PreferenceScreen>
